## To do

### Code

* `estimate_beta()` wrapper
* `make_par_list()`,  `make_data()` with random reporting delays
* smoothing wrapper
* print, plot, summary methods

### Debug

### Documentation

* `impute()` functions
* vignette
